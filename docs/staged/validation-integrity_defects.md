# Staged defects — branch `fix/validation-integrity`, 17 September 2026

Staged here, **not** in `docs/defect_register.md`, for the main session to
integrate. Numbers 51–53 were assigned to this branch; nothing else is
numbered. No "N found / N fixed" count anywhere has been changed.

| # | Defect | How it showed up | State |
|---|---|---|---|
| 51 | **The Almería calibration data counted 18 measurements twice, and the holdout contained a training row** | `dataset.load_all()` concatenated `Exp1.nc`, `Exp2.nc` and `Exp3.nc` into **165** rows, of which only **147** are distinct across the seven measured channels. All 17 rows of `Exp3.nc` are exact copies of `Exp1.nc` rows 0–16, in order — the dataset's own README describes Exp3 as a *different* full-factorial campaign, so the file does not contain that campaign — and Exp1 row 16 is also Exp2 row 113. The 50-row V1/V2 holdout was **33 distinct rows**, one of which the fill law was trained on. V1 heat rejection had "passed" at **5.94 %** against 6.00 % on it. Found by an independent review and confirmed here from the files. The same duplication sits under every per-campaign analysis that treats Exp3 as a campaign. | **Fixed** — 17 Sep 2026, re-scored as pre-registered in `docs/staged/almeria_dedup_preregistration.md` (committed first). `load_all()` de-duplicates and records campaign membership; `split_holdout()` cannot hold out a training row; training is unchanged (115 Exp2 rows, fill law identical to 1e-12); holdout **50 → 32**. Thresholds untouched: V1 outlet MAE **0.542 → 0.600 K, PASS**; V1 heat rejection **5.94 % PASS → 6.27 % FAIL**; V2 evaporation **9.90 % → 10.80 %, FAIL**. Leave-one-campaign-out reported as a diagnostic that cannot change a verdict. Held by `tests/test_dataset_dedup.py` |
| 52 | **The README's results table reported a failed gate as passed, and no guard read the README** | `README.md` — the front page of the public repository — said *V2 evaporation vs measured water loss ≤ 8.00 % \| 7.11 % \| PASS* while `results/calibration.json` held **9.90 %, FAIL**; the V5 cost (5.60 %), V5 water (10.04 %), V3 (15.6 %) and V5b (7 and 10 cycles, gypsum) rows were stale too. `7.11 %` is on the audit's own list of superseded figures, but `src/audit.py` section 5 scanned only `docs/*.md`, `docs/*.txt` and `HANDOFF.md`, section 6 checked claims only in `poc_report.md`, and `tests/test_artefact_consistency.py` compared no README row with an artefact. Same shape as defect 13: a guard whose glob stopped short of the document with the widest audience. Also found in passing: section 6's `"9.90 %"` literal would have **demanded the superseded V2 figure** once defect 51 moved it (defect 12's shape), and the variable `v2` is rebound to a `Path` in section 4c, so any later use of it as a verdict is always true. | **Fixed** — README rows rewritten from this session's `calibrate.py` and `run_controller.py` output. `README.md` added to the section 5 scan; new section 6b checks every README gate row's number and verdict against the artefacts; the `9.90 %` literal replaced by the artefact value with a recomputed verdict. Both guards were shown non-vacuous by running them against the old README (audit raised 8; the new test failed). `CITATION.cff`'s "Four pass and two fail" updated to three and three |
| 53 | **"Benchmarked against PHREEQC 3.9.0" rested on one composition, and a pre-registered multi-composition benchmark fails** | The engine's only comparison with PHREEQC was the USGS gypsum-saturation example (one water, 25 °C, one mineral: −0.0163). Pre-registered grid (`docs/staged/phreeqc_benchmark_preregistration.md`, committed before running): 4 waters × cycles 1–8 × 25/35/45/55 °C, identical mol/kgw totals, fixed pH, no charge adjustment in either code, calcite/gypsum/SiO₂(a)/hydroxyapatite. **398 of 416** gated indices (95.7 %) within tolerance, but **calcite 114/128 = 89.1 %** against a 90 % per-mineral bar: **FAIL**. All 18 failures are at I ≤ 0.1 and 45–55 °C; worst calcite −0.098 (DOE SynMWW_NF, 4 cycles, 55 °C). Inputs checked first (totals round-trip to 1e-7; ionic strength agrees within 5 %). At the worst point: −0.041 from Davies vs Truesdell–Jones activity coefficients, −0.033 from a calcite constant transcribed from an **earlier phreeqc.dat** than 3.9.0 ships, −0.006 from complexation. Gypsum at 55 °C passes largely by cancellation: log K is 0.106 above 3.9.0 while free Ca·SO₄ is ~0.10–0.12 log units above PHREEQC's. Hydroxyapatite passes at 4× tolerance but only 39.8 % within the unscaled one. | **OPEN as a result, wording fixed** — the claim is replaced by the measured statement in the deck, the external handoff, the PoC report generator, `brand.py`, `ceiling_report.py`, `chemistry.py`'s module docstring and the README. `tests/test_phreeqc_grid.py` compares the live engine with the stored PHREEQC output: the criterion is `xfail(strict=True)` against this defect, and a non-xfail test pins the shares and where the failures sit. Whether to update the engine's constants and activity model to 3.9.0 is a physics decision left open |
| 57 *(number assigned by the incumbent branch, not re-numbered here)* | **Brucite saturation pH paired a hydroxide-form log K with a proton-form enthalpy** | `ph_saturation_brucite` used `_vant_hoff(-11.18, -27.1, T)`: -11.18 is `Mg(OH)2 = Mg+2 + 2 OH-`, but -27.1 kcal is the enthalpy of `Mg(OH)2 + 2 H+ = Mg+2 + 2 H2O` (wateq4f.dat). The two agree at 25 C, so only a temperature sweep sees it. Supplementary pre-registration (`docs/staged/phreeqc_brucite_supplement_preregistration.md`, committed before running; phreeqc.dat has no Brucite phase, so PHREEQC + wateq4f.dat from the same installer): **0 of 96** temperature shifts within 0.05 pH; median D -0.321 / -0.623 / -0.907 pH at 35 / 45 / 55 C, against a predicted -0.32 / -0.62 / -0.90. Confirmed. | **Fixed** -- hydroxide-form enthalpy -27.1 + 2 x 13.362 = -0.376 kcal, log K(25 C) unchanged. Re-scored against the same stored run: **96 of 96**, medians -0.004 / -0.007 / -0.012. `results/mg_silicate_envelope.json` re-run: skin saturation pH 8.92 -> 9.33 at 38 C, 8.45 -> 9.09 at 46 C, 8.22 -> 8.98 at 50 C; 38-46 C move from depositing/straddling to safe across the 8.5-9.0 band. V5/V7 re-run unchanged (4.38 % water FAIL, 3.91 % cost PASS, 0 violations). NOT re-run: the MATLAB/Simulink twin and `results/matlab_simulink.json` (the audit still checks its brucite limit 8.43-8.89), figures, and prose quoting the old limits. Absolute pH_sat still sits 0.06-0.19 below PHREEQC because the function uses total Mg and unspeciated ionic strength (reported, not gated). Held by `tests/test_phreeqc_grid.py` |

## Unnumbered — found in passing, not fixed here

| Defect | How it showed up |
|---|---|
| **V7 overwrites the V5 results table on every full run** | `run_controller.gate_v5()` writes `results/v5_controller.csv` itself, and V7 calls `gate_v5(..., baseline_cycles=3.0, label="V7")`, so after `python src/run_controller.py` the file holds the **3-cycle** baseline rows. Regenerating `poc_report.md` then prints V7 rows under the V5 heading ("3 → 5", +19.2 % water). The committed CSV (4-cycle rows) was restored rather than committed. Not fixed here because V5/V7 baseline code is another branch's territory. |
| **Artefacts computed on the duplicated data remain** | `results/drift.json`, `drift.csv`, `uncertainty.json`, `fill_law_selection.csv`, `fill_law_decision.json`, `v2_diagnosis.json` and the per-campaign constants typed into `src/gate_uncertainty.py` / `scripts/gate_uncertainty.py` (Exp3 n = 17, offset −14.59 %) predate defect 51. The consumers now use the de-duplicated loader but were not re-run. The audit's "fill drift across campaigns" check (15–35 % spread) includes the Exp3 subset as if it were a campaign. |
| **The PoC report generator is behind the committed report** | Regenerating `docs/poc_report.md` with `src/make_report.py` reverts hand-made corrections (e.g. "15 % requires roughly 8.5 cycles and amorphous silica" back to "...and gypsum saturates") and rewrites figure paths to the local checkout, although the Layout note says `docs/` is never hand-edited. Only the defect-51/53 lines were taken from the regeneration. |
| **`balance_sodium` counts phosphate as −3** | It uses the formal `SPECIES` charge while `charge_balance_pct` uses `phosphate_mean_charge(pH)` (≈ −1.8 at pH 7.8), so a water "balanced" by one is not balanced by the other (~0.5 % at 8 mg/L PO₄). |

## Documents this branch was not allowed to edit, which carry now-superseded statements

* `HANDOFF.md:244` — "4.4–4.9 max cycles … landing on observed practice"; the corrected engine computes **5.84**, above the 3.5–5.0 band. `HANDOFF.md:325` — "165 steady-state points".
* `docs/robustness_gaps.md:410–413` — "lands on observed industry practice: … ~5.8 cycles against a Gulf empirical band of 3.5–5.0"; 5.84 is above the band, not on it.
* `docs/defect_register.md:119` — "all 165 points"; Part 2 V1/V2 figures (0.542 K, 5.94 %, 9.90 %) and any "four pass, two fail".
* Also still quoting 0.542 K / 5.94 % / 9.90 % or "four pass, two fail" (not edited here, see `git grep`): `docbuild/build_answers.js:25`, `docbuild/build_defects.js:58`, `docbuild/build_index.js:44`, `docs/application_answers.md`, `docs/deck_2026_09_12.md`, `docs/model_robustness.md`, `docs/threshold_revision_memo.md`, `docs/v7_preregistration.md`, `src/package.py:84`, `src/pinn.py:84`, `scripts/gate_uncertainty.py:17`. The audit's superseded-number scan cannot flag them until the register's before/after table records 5.94 → 6.27 and 9.90 → 10.80.
* `CITATION.cff` still says "a register of ten defects" and carries `version: 1.0.0`, `date-released: 2026-08-30`; left, per the rule on counts and because a version bump is a release decision.
